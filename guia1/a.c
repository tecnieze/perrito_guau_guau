#include <stdio.h>
float valor_dolar,valor_peso,valor_reales; 
char m[1];
float cant_inicial,cant_final;
int main(void) {
   printf("elija entre pesos(P),reales(R),dolares(D) y euros(E) que desea convertir  \n");
   scanf("%c" , m);
  
   printf("indique  la a covertir")
  
   printf("%s \n", m);
  return 0;
}